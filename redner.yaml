services:
  - type: web
    name: tradevault-frontend
    env: static
    staticPublishPath: dist/trade-vault-frontend
    buildCommand: npm install && npm run build
    publishCommand: npm run build
